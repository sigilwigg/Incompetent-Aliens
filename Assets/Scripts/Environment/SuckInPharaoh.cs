using UnityEngine;

public class SuckInPharaoh : MonoBehaviour
{
    /*
     * vars
     *      REFERENCES
     *          pharaoh ref
     *          tornado prefab ref
     *          glyph slot ref
     *
     * 
     * if all glyph slots in place
     *      disable pharaoh
     *      enable tornado
     *      move tornado to sacrophagas
     *      
     *      if tornado is in sacrophagas
     *          disable tornado
     *          paly close lid anim
     * 
     */
}
